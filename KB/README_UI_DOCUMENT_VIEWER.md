# Document Viewer - Интерфейс детального просмотра документов

## Описание

Новый интерфейс для работы с документами, предоставляющий удобный просмотр файла, его чанков и AI Items в одном месте.

## Основные возможности

### 1. Трёхвкладочный интерфейс
- **Файл** - просмотр исходного текста документа
- **Чанки** - список всех чанков с фильтрацией
- **AI Items** - связанные AI Items

### 2. Просмотр больших текстов
- Чанки отображаются с превью (первые 200 символов)
- Кнопка "👁️" открывает полный текст в модальном окне
- Кнопка "⛶" для развёртывания текста файла на весь экран

### 3. Фильтрация чанков
- По уровню (0-исходник, 1-связи, 2-логика)
- По типу (function, class, table, view и т.д.)
- По тексту (поиск)

### 4. Действия с файлом
- **Удалить файл** - удаление файла и всех связанных данных
- **Изменить контекст** - смена контекстного кода
- **Ревекторизация** - перезапуск процесса векторизации
- **Создать AI Item** - создание нового AI Item из чанка

### 5. Работа с AI Items
- Просмотр всех чанков AI Item по уровням (L0, L1, L2)
- Информация о AI Item (ID, имя, тип, контекст)
- Создание чанков L1 и L2
- Удаление AI Item

## Дизайн

### Темная тема
- Фон: `#0d1117` (GitHub Dark)
- Панели: `#161b22`
- Текст: `#c9d1d9`
- Акцент: `#58a6ff`

### Компактность
- Минимальные отступы (8px)
- Мелкие шрифты для метаданных (11-12px)
- Оптимизированное использование пространства

## Технические детали

### Файлы
- `public/document-viewer.html` - разметка интерфейса
- `public/document-viewer.css` - стили (темная тема, компактный дизайн)
- `public/document-viewer.js` - логика (загрузка данных, модальные окна, фильтры)

### API Endpoints
- `GET /file-info/:filename` - информация о файле
- `GET /file-content/:filename` - содержимое файла
- `GET /file-chunks/:filename` - чанки файла
- `GET /ai-items` - все AI Items
- `GET /ai-item/:id` - конкретный AI Item
- `GET /ai-item/:id/chunks?level=...` - чанки AI Item по уровням
- `DELETE /file/:filename` - удаление файла
- `POST /update-context/:filename` - обновление контекста

### URL параметры
`document-viewer.html?filename=<имя_файла>`

Например: `document-viewer.html?filename=example.js`

## Использование

### Открытие интерфейса
1. На главной странице в списке файлов нажать кнопку "📄 Детали"
2. Или перейти напрямую по URL с параметром `filename`

### Просмотр текста
- В превью чанка видно первые 200 символов
- Нажать кнопку "👁️" для просмотра полного текста в модальном окне
- В модальном окне можно скопировать текст кнопкой "Копировать"

### Фильтрация чанков
1. Перейти на вкладку "Чанки"
2. Использовать фильтры:
   - Выбрать уровень из выпадающего списка
   - Выбрать тип из выпадающего списка
   - Ввести текст для поиска

### Работа с AI Items
1. Перейти на вкладку "AI Items"
2. Нажать на карточку AI Item
3. В модальном окне:
   - Переключаться между уровнями (L0, L1, L2)
   - Просматривать чанки каждого уровня
   - Создавать новые чанки L1/L2
   - Удалить AI Item

## Будущие улучшения

### Планируется добавить:
- **Граф связей** - визуализация parent_chunk_id
- **Diff viewer** - сравнение чанков разных уровней
- **Массовые операции** - выделение и удаление нескольких чанков
- **Экспорт** - сохранение чанков в файл
- **Подсветка синтаксиса** - для кода в превью
- **Drag & Drop** - для изменения порядка чанков
- **История изменений** - отслеживание изменений в чанках

## Адаптивность

Интерфейс адаптируется под разные размеры экрана:
- На мобильных устройствах (< 768px) вкладки и фильтры располагаются вертикально
- Модальные окна занимают 90% ширины экрана
- Сохраняется удобство использования на всех устройствах

