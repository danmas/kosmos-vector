# --- Server Configuration ---
PORT=3005
BASE_URL=localhost

# --- Directories ---
# Directory for source documents to be vectorized
DOCS_DIR=./docs
# Directory for output documents (e.g., exported chunks)
OUTPUT_DOCS_DIR=./output_docs

# --- PostgreSQL Connection ---
# Use DATABASE_URL for platforms like Supabase/Heroku, or individual PG variables
# DATABASE_URL=postgres://user:password@host:port/database
PGHOST=127.0.0.1
PGPORT=5432
PGDATABASE=postgres
PGUSER=postgres
PGPASSWORD=your_postgres_password

# --- Embeddings Model ---
# Set to true to use OpenAI embeddings, false for local 'simple' embeddings
USE_OPENAI=false
# Required only if USE_OPENAI is true
OPENAI_API_KEY=sk-...

# --- External AI Server for RAG answers ---
# The endpoint of the server that will generate the final answer
REQ_SERVER_URL=http://localhost:3002
# Default model name to be used for generation
DEFAULT_MODEL_NAME=google/gemini-2.0-flash-exp:free
# Default number of similar chunks to retrieve for context
MAX_RESULTS=5

# --- SQL L1/L2 Prompt Templates ---
# Templates for generating Level 1 (dependencies) chunks for SQL functions
SQL_L1_FUNCTION_PROMPT=Проанализируй эту SQL функцию/процедуру и опиши её зависимости от других объектов базы данных.
SQL_L1_FUNCTION_INPUT_TEXT=Какие другие функции, процедуры или таблицы использует этот код? Опиши связи между объектами.

# Templates for generating Level 2 (business logic) chunks for SQL functions
SQL_L2_FUNCTION_PROMPT=Опиши бизнес-логику этой SQL функции/процедуры простым языком.
SQL_L2_FUNCTION_INPUT_TEXT=Что делает эта функция? Какую бизнес-задачу она решает? Опиши алгоритм работы.

# Templates for generating Level 1 chunks for SQL tables
SQL_L1_TABLE_PROMPT=Опиши связи этой таблицы с другими объектами базы данных.
SQL_L1_TABLE_INPUT_TEXT=Какие таблицы ссылаются на эту через внешние ключи? С какими таблицами она связана?

# Templates for generating Level 2 chunks for SQL tables
SQL_L2_TABLE_PROMPT=Опиши назначение этой таблицы в системе.
SQL_L2_TABLE_INPUT_TEXT=Какие данные хранятся в этой таблице? Для каких бизнес-процессов она используется?

# --- JavaScript L1/L2 Prompt Templates ---
# Templates for JavaScript functions
JS_L1_FUNCTION_PROMPT=Проанализируй эту JavaScript функцию и опиши её зависимости.
JS_L1_FUNCTION_INPUT_TEXT=Какие другие функции она вызывает? Какие модули импортирует? К каким данным обращается?

JS_L2_FUNCTION_PROMPT=Опиши бизнес-логику этой JavaScript функции простым языком.
JS_L2_FUNCTION_INPUT_TEXT=Что делает эта функция? Какую задачу она решает? Опиши алгоритм работы.

# Templates for JavaScript classes
JS_L1_CLASS_PROMPT=Проанализируй этот JavaScript класс и опиши его зависимости.
JS_L1_CLASS_INPUT_TEXT=Какие другие классы он использует? Какие интерфейсы реализует? Какие методы экспортирует?

JS_L2_CLASS_PROMPT=Опиши назначение этого JavaScript класса.
JS_L2_CLASS_INPUT_TEXT=Какую роль играет этот класс в системе? Какую функциональность он предоставляет?

# --- Markdown L1/L2 Prompt Templates ---
# Templates for Markdown sections
MD_L1_SECTION_PROMPT=Проанализируй эту секцию документации и опиши её связи с другими частями проекта.
MD_L1_SECTION_INPUT_TEXT=Какие другие секции документации или код связаны с этой секцией?

MD_L2_SECTION_PROMPT=Кратко опиши содержание этой секции документации.
MD_L2_SECTION_INPUT_TEXT=О чём эта секция? Какую информацию она содержит?