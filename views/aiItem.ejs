<div class="ai-item-chunk" data-type="${chunk.type}" data-s-name="${chunk.s_name}">
    <div class="ai-item-chunk-header" data-chunk-id="${chunk.id}">
        <div class="ai-item-chunk-title">${chunk.title || 'Чанк ' + (index + 1)}</div>
        <button class="save-chunk-btn">Сохранить в файл</button>
    </div>
    <div class="ai-item-chunk-content">${chunk.content}</div>
</div> 